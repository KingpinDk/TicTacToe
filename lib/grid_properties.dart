import 'dart:ui';
import 'package:flutter/material.dart';
//by DK

class GridProperties{
  String str = " ";
  Color gridColor = Colors.black12;
  late int x, y;
  GridProperties(this.gridColor, this.str, this.x, this.y);
}